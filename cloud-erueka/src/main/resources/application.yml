server:
  port: 51010
spring:
  application:
    name: cloud-eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:51010/eureka/
  server:
    enable-self-preservation: false
